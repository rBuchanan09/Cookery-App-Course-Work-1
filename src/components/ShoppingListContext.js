import { createContext } from "react";

/*
    This component is responsable for creating a context for the create shopping list page
*/

const ShoppingListContext = createContext();
export default ShoppingListContext;